{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm run server",
    "server": "cross-env PORT=3000 json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "json-server": "^0.17.3",
    "path": "^0.12.7"
  }
}
