{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm run server",
    "server": "cross-env PORT=3000 json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}