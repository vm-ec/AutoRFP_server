{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3007"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "json-server": "^0.17.3",
    "path": "^0.12.7"
  }
}
